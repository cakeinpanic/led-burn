name: 'On push'
on:
  push:
    branches:
      - '**'
    tags:
      - '!**'

jobs:
  cypress-test-on-docker:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-node@v1
        name: setup node
        with:
          node-version: '14.x'

      - name: Cache node modules
        uses: actions/cache@v2
        with:
          path: node_modules
          key: cache-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            cache-

      - name: 'install'
        run: npm i

      - name: 'build'
        run: npm run build

      - run: git branch -d pages
